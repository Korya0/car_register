name: car_register_app
description: "Car Registration App"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.0
  go_router: ^14.6.2
  gsheets: ^0.5.0
  http: ^1.2.1
  connectivity_plus: ^5.0.2
  shared_preferences: ^2.2.3
  google_fonts: ^6.3.0
  flutter_screenutil: ^5.9.3
  animate_do: ^4.2.0
  device_preview: ^1.3.1
  flutter_native_splash: ^2.4.6
  logger: ^2.5.0
  get_it: ^8.0.3
  skeletonizer: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/google_sheets_credentials.json
